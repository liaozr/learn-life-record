export const commonParams = {
  g_tk: 1928093487,
  inCharset: 'utf-8',
  outCharset: 'utf-8',
  notice: 0,
  format: 'jsonp'
}

// 配置callback的名字
export const options = {
  param: 'jsonpCallback'
}

// 配置一个常量
export const ERR_OK = 0

// 配置音乐播放器的播放模式
export const playMode ={
  sequence:0,
  loop:1,
  random:2
}